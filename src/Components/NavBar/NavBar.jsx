import React from "react";
import { Menu } from "semantic-ui-react";

const NavBar = () => (
  <Menu inverted>
    <Menu.Item>
      <a href="#">Home</a>
    </Menu.Item>
  </Menu>
);

export default NavBar;
